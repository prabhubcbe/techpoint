export interface DataModel {
    letter: string;
    frequency: number;
}

export interface RadialDataModel {
  category: string;
  current_result: number;
}

export interface BubbleChartModel {
  score: number; email: any; branch: any; dept: any;
}
